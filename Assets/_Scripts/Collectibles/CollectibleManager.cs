using UnityEngine;

namespace _Scripts.Collectibles
{
    public enum CollectibleType
    {
        
    }


    public class CollectibleManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}